<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:bclocate="clr-namespace:CoBCCanteen.ViewModels"
             x:Class="CoBCCanteen.App">
    <!--
        Define global resources and styles here, that apply to all pages in your app.
    -->

    <Application.Resources>
        <Color x:Key="PrimaryBlue">#00a9e0</Color>
        <Color x:Key="PrimaryGrey">#888b8d</Color>
        <Color x:Key="PrimaryBlack">#000000</Color>
        <Color x:Key="PrimaryWhite">#ffffff</Color>

        <Color x:Key="SecondaryNavy">#003349</Color>
        <Color x:Key="SecondaryAqua">#68d2df</Color>
        <Color x:Key="SecondaryIndigo">#005587</Color>
        <Color x:Key="SecondaryLime">#c5e86c</Color>
        <Color x:Key="SecondaryMint">#279989</Color>
        <Color x:Key="SecondaryPeach">#f8ad6d</Color>
        <Color x:Key="SecondaryPink">#ff5b73</Color>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="TextColor" Value="{StaticResource PrimaryWhite}"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="{StaticResource SecondaryPink}"/>
            <Setter Property="TextColor" Value="{StaticResource PrimaryWhite}"/>
        </Style>

        <Style x:Key="PrimaryLabelLinkStyle" TargetType="Label">
            <Setter Property="TextColor" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="TextDecorations" Value="Underline"/>
            <Setter Property="HorizontalOptions" Value="Center"/>
        </Style>

        <Style x:Key="InvalidEntryStyle" TargetType="Entry" >
            <Setter Property="TextColor" Value="{StaticResource SecondaryPink}" />
        </Style>

        <Style x:Key="Separator" TargetType="BoxView">
            <Setter Property="HeightRequest" Value="1" />
            <Setter Property="WidthRequest" Value="50"/>
            <Setter Property="VerticalOptions" Value="Center"/>
            <Setter Property="HorizontalOptions" Value="Center" />
            <Setter Property="Color" Value="{StaticResource PrimaryGrey}" />
            <Setter Property="Margin" Value="0, 5, 0, 5" />
            <Setter Property="Opacity" Value="0.5" />
        </Style>

        <xct:MathExpressionConverter x:Key="MathExpressionConverter" />
    </Application.Resources>
</Application>

