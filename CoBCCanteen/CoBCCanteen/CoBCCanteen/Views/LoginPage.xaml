<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:CoBCCanteen.ViewModels"
    x:Class="CoBCCanteen.Views.LoginPage"
    x:DataType="viewmodels:LoginPageViewModel"
    BackgroundColor="White"
    Shell.NavBarIsVisible="False"
    Shell.TabBarIsVisible="False"
    Shell.FlyoutBehavior="Disabled">
    <ContentPage.BindingContext>
        <viewmodels:LoginPageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HorizontalOptions="FillAndExpand" Padding="40" >
            <Image Source="HeaderCoBC" HorizontalOptions="Center" HeightRequest="75" />
            <Label Text="Canteen Ordering App" FontSize="28" HorizontalOptions="Center" Margin="0, 25, 0, 10" />
            <Entry x:Name="txtID" Text="{Binding ID}" Placeholder="Student ID" Keyboard="Numeric" />
            <Entry x:Name="txtPassword" Text="{Binding Password}" Placeholder="Password" IsPassword="True" />
            <Button Command="{Binding LoginUser}" Text="Login" Style="{StaticResource PrimaryButtonStyle}" Margin="0, 5, 0, 25" />
            <Label Text="Don't have an account? Register here" Style="{StaticResource PrimaryLabelLinkStyle}">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding DisplayRegister}"/>
                </Label.GestureRecognizers>
            </Label>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

