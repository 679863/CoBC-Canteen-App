<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:CoBCCanteen.ViewModels"
    x:DataType="viewmodels:TopupPageViewModel"
    x:Class="CoBCCanteen.Views.TopupPage"
    Title="Topup">
    <ContentPage.BindingContext>
        <viewmodels:TopupPageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="Center" Padding="25">
            <Label Text="{Binding DisplayBalance}"/>
            <Label Text="{Binding DisplayTopupValue}"/>
            <Slider Value="{Binding SliderTopupValue}" x:Name="SliderValue" Minimum="0" Maximum="30" ValueChanged="OnSliderValueChanged"/>
            <Label Text="Payment details"/>
            <Entry Placeholder="Card number"/>
            <Entry Placeholder="Expiry date"/>
            <Entry Placeholder="CVV"/>
            <Button Text="Topup" Style="{StaticResource PrimaryButtonStyle}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

